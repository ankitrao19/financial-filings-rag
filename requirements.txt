# Direct dependencies of pipeline/, app/ and tests/ (pinned to the versions in venv).
# Install: venv/bin/pip install -r requirements.txt

# --- data: fetch filings from SEC EDGAR (build_chunks.py --fetch)
edgartools==5.45.0

# --- embeddings + vector store
sentence-transformers==5.7.0
transformers==5.14.1
torch==2.13.0
einops==0.8.2              # required by nomic-embed-text-v1.5 (trust_remote_code)
chromadb==1.5.9

# --- LLM (answer + judge) and config
openai==3.8.0
python-dotenv==1.2.2

# --- API
fastapi==0.141.1
uvicorn==0.52.1
pydantic==2.13.4

# --- tracing
langfuse==4.15.4

# --- UI
gradio==6.27.0
httpx==0.28.1
